{
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "name": "MonitoreoBH",
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "prebuild": "npm run prebuild --workspaces --if-present",
    "pretest": "npm run prebuild --workspaces --if-present",
    "start": "npm start --workspaces --if-present",
    "test": "npm test --workspaces --if-present"
  },
  "type": "module",
  "workspaces": [
    "src/MonitoreoBH_backend",
    "src/MonitoreoBH_frontend"
  ],
  "dependencies": {
    "apiip.net": "^1.3.0",
    "axios": "^1.7.7",
    "bootstrap": "^5.3.3",
    "express": "^4.21.1",
    "leaflet": "^1.9.4",
    "react": "^18.3.1",
    "react-bootstrap": "^2.10.5",
    "react-leaflet": "^4.2.1",
    "react-router-dom": "^6.27.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0"
  }
}
